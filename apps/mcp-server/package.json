{
  "name": "@carely/loop-mcp-server",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@carely/loop-client": "workspace:*",
    "@carely/loop-shared": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.17.4",
    "zod": "^3.24.4"
  }
}
